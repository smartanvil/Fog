"
Subclass of set that just does not save anything. 
"
Class {
	#name : #FogFakeSet,
	#superclass : #Set,
	#category : 'FogDB-Cache'
}

{ #category : #'as yet unclassified' }
FogFakeSet >> add: anItem [
	" Does Nothing :) "
]
